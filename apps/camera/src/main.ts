// Composition root for the Sentinel Monitor camera publisher.
// PR4 fills this in. For now, just a marker so the dev server boots.

document.getElementById('app')!.innerHTML = `
  <div style="font-family: system-ui; padding: 32px; color: #e8eef5; background: #0a0d12; min-height: 100vh;">
    <h1 style="font-size: 32px;">Sentinel Camera</h1>
    <p>Scaffold ready. PR4 will land the publisher.</p>
  </div>
`;
