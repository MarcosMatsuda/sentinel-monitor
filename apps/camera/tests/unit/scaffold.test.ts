import { describe, expect, test } from 'vitest';

// Placeholder smoke test so Vitest has at least one test to run on
// scaffold. Real domain tests land in PR4. Delete this file once
// any real test exists in tests/unit/.
describe('scaffold', () => {
  test('vitest is wired and runs', () => {
    expect(true).toBe(true);
  });
});
